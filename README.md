# Socket Simplex

A simple app to send simple Unicode messages via Java Sockets. Send between two local devices running the app or between the app and another socket client. This app is an experiment in Java Sockets library. 
